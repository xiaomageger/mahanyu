<mapper namespace="user" xmlns="http://mybatis.org/schema/mybatis-mapper">
    <select id="userInfo" parameterType="integer" statementType="PREPARED" resultType="list">
        select * from user_info where id = # {id}
    </select>
</mapper>